---
phase: 04-timing-reconciliation
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - breeze.go
  - processor/fruit.go
autonomous: true
requirements:
  - TIME-01
  - TIME-02

must_haves:
  truths:
    - "`resolveGrademapID` exists in breeze.go and compiles"
    - "Config has `GrademapPropagationDelayStr string` field"
    - "`Fruit` struct has `GrademapID *int64` field"
    - "`Columns()` returns `grademap_id` as the last element"
    - "`AsRow()` appends `a.GrademapID` as the last value"
    - "TestConfig_PropagationDelay, TestResolveGrademapID_* stubs now compile and pass"
  artifacts:
    - path: "breeze.go"
      provides: "resolveGrademapID function, Config.GrademapPropagationDelayStr field"
      exports: ["resolveGrademapID"]
    - path: "processor/fruit.go"
      provides: "Fruit.GrademapID field, updated Columns() and AsRow()"
      contains: "GrademapID"
  key_links:
    - from: "breeze.go resolveGrademapID"
      to: "breeze_grademap.received_at"
      via: "SELECT id FROM breeze_grademap WHERE received_at <= $1 ORDER BY received_at DESC LIMIT 1"
      pattern: "resolveGrademapID"
    - from: "processor/fruit.go Columns()"
      to: "breeze_fruit.grademap_id"
      via: "last element of Columns() slice"
      pattern: "grademap_id"
---

<objective>
Implement the core timing reconciliation logic: `resolveGrademapID` function in `breeze.go`, the `GrademapPropagationDelayStr` config field, and the `GrademapID *int64` field on the `Fruit` struct with matching `Columns()` and `AsRow()` updates.

Purpose: This plan provides all the building blocks. Plan 03 wires them into `writeBatch` and the schema DDL.

Output:
- `resolveGrademapID(ctx, pool, fruitTime, delay)` in `breeze.go`
- `Config.GrademapPropagationDelayStr string` in `breeze.go`
- `Fruit.GrademapID *int64` in `processor/fruit.go`
- `Columns()` and `AsRow()` updated to include `grademap_id` as the last column
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-timing-reconciliation/04-RESEARCH.md
@.planning/phases/04-timing-reconciliation/04-01-SUMMARY.md

<interfaces>
<!-- Existing pool.QueryRow pattern from breeze.go writeGrademap() — use verbatim as template -->
err := pool.QueryRow(ctx,
    `INSERT INTO breeze_grademap (received_at, payload) VALUES ($1, $2) RETURNING id`,
    time.Now(),
    payload,
).Scan(&grademapID)

<!-- Existing Config struct (breeze.go lines 40-52): -->
type Config struct {
    MQTT struct {
        URI  string `yaml:"uri"  envconfig:"SERVER_URI"`
        User string `yaml:"user" envconfig:"SERVER_USER"`
        Pass string `yaml:"pass" envconfig:"SERVER_PASS"`
    } `yaml:"mqtt"`
    Database struct {
        URL string `yaml:"url" envconfig:"DATABASE_URL"`
    } `yaml:"database"`
    LogLevel        string `yaml:"log_level"         envconfig:"LOG_LEVEL"`
    QueueSize       int    `yaml:"queue_size"         envconfig:"QUEUE_SIZE"`
    WriteBufferSize int    `yaml:"write_buffer_size"  envconfig:"WRITE_BUFFER_SIZE"`
    // ADD: GrademapPropagationDelayStr string
}

<!-- Existing Fruit struct (processor/fruit.go) ends with: -->
PrimaryDefect  string
OtherDefects   []string
PrimaryReason  string
OtherReason    []string
// ADD: GrademapID *int64  (nil = no matching grademap row)

<!-- Columns() returns the last entries as: -->
"primary_defect", "other_defects", "primary_reason", "other_reasons"
// ADD "grademap_id" as the final element

<!-- AsRow() returns the last values as: -->
a.PrimaryDefect, a.OtherDefects, a.PrimaryReason, a.OtherReason
// ADD a.GrademapID as the final value (nil *int64 → SQL NULL)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resolveGrademapID and GrademapPropagationDelayStr to breeze.go</name>
  <files>breeze.go</files>
  <behavior>
    - resolveGrademapID(ctx, nil-or-closed pool, fruitTime, 0) → returns error (not panic), ok=false
    - resolveGrademapID with pgx.ErrNoRows → returns (0, false, nil) — no grademap yet is not an error
    - resolveGrademapID with non-zero delay → subtracts delay from fruitTime before query
    - Config.GrademapPropagationDelayStr parses with time.ParseDuration without error for "30s" and "0s"
    - Config.GrademapPropagationDelayStr empty string → treated as zero delay in run() (Plan 03)
  </behavior>
  <action>
    1. Add `GrademapPropagationDelayStr string` to the `Config` struct after `WriteBufferSize`:
       ```go
       GrademapPropagationDelayStr string `yaml:"grademap_propagation_delay" envconfig:"GRADEMAP_PROPAGATION_DELAY"`
       ```

    2. Add the `resolveGrademapID` function after `writeGrademap`. Use the exact signature:
       ```go
       func resolveGrademapID(ctx context.Context, pool *pgxpool.Pool, fruitTime time.Time, delay time.Duration) (int64, bool, error)
       ```
       Implementation:
       - adjustedTime := fruitTime.Add(-delay)
       - var id int64
       - err := pool.QueryRow(ctx, `SELECT id FROM breeze_grademap WHERE received_at <= $1 ORDER BY received_at DESC LIMIT 1`, adjustedTime).Scan(&id)
       - if errors.Is(err, pgx.ErrNoRows) { return 0, false, nil }
       - if err != nil { return 0, false, fmt.Errorf("resolve grademap id: %w", err) }
       - return id, true, nil

    3. Add "errors" to the import block (it is not yet imported).

    Do NOT modify writeBatch, run(), or any other function — those are Plan 03 changes.

    Anti-pattern note: do NOT use pgx.ErrNoRows as a fatal error. The no-rows case is valid
    (Breeze may start receiving fruit before any grademap arrives).
  </action>
  <verify>
    <automated>cd /home/ben/github.com/bellgrove/breeze && go test . -run "TestResolveGrademapID|TestConfig_PropagationDelay" -count=1 -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    `resolveGrademapID` compiles. TestResolveGrademapID_NoRows, TestResolveGrademapID_ZeroDelay,
    TestResolveGrademapID, TestResolveGrademapID_WithDelay, TestConfig_PropagationDelay,
    TestConfig_PropagationDelayZero all compile and pass (or pass with t.Skip for DB-dependent
    assertions). `go test ./...` is green (TestWriteBatch_GrademapID remains RED until Plan 03).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GrademapID field to Fruit struct and update Columns/AsRow</name>
  <files>processor/fruit.go</files>
  <behavior>
    - Fruit struct has GrademapID *int64 field
    - Columns() returns "grademap_id" as the last element (len increases by 1)
    - AsRow() returns a.GrademapID as the last value (nil *int64 maps to SQL NULL via pgx)
    - Columns() and AsRow() remain in sync — same count, same order
  </behavior>
  <action>
    1. Add `GrademapID *int64` as the last field of the `Fruit` struct (after `OtherReason []string`):
       ```go
       GrademapID *int64 // nil when no matching breeze_grademap row
       ```

    2. Append `"grademap_id"` as the last element in the `Columns()` return slice.

    3. Append `a.GrademapID` as the last value in the `AsRow()` return slice.
       pgx handles a nil *int64 as SQL NULL automatically — no special casing needed.

    Do NOT change any other fields or functions in this file.

    Verify column count matches row value count after the change. The existing fruit tests
    in processor/ should still pass — Columns() and AsRow() are only tested for shape, not
    exact content, in the existing test suite.
  </action>
  <verify>
    <automated>cd /home/ben/github.com/bellgrove/breeze && go test ./processor/... -count=1 -timeout 30s 2>&1</automated>
  </verify>
  <done>
    `processor/fruit.go` compiles. `go test ./processor/...` is green. Fruit.GrademapID exists
    as *int64. Columns() length increased by 1. AsRow() length matches Columns() length.
    `go test ./...` is green except TestWriteBatch_GrademapID (RED until Plan 03 updates
    writeBatch signature).
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `go test ./processor/...` green
- `go test . -run "TestResolveGrademapID|TestConfig_PropagationDelay" -count=1` green
- `grep "GrademapID" processor/fruit.go` shows field, Columns entry, and AsRow entry
- `grep "resolveGrademapID\|GrademapPropagationDelayStr" breeze.go` shows both symbols
</verification>

<success_criteria>
`resolveGrademapID` implemented in breeze.go. `Config.GrademapPropagationDelayStr` field added. `Fruit.GrademapID *int64` field added with matching Columns() and AsRow() updates. All tests except TestWriteBatch_GrademapID are green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-timing-reconciliation/04-02-SUMMARY.md`
</output>

---
phase: 01-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - processor/processor_test.go
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "Failing tests exist for the three QUAL-01/02/03 behaviors before any implementation"
    - "go test ./processor/... compiles and runs (tests fail at assertion, not compile error)"
  artifacts:
    - path: "processor/processor_test.go"
      provides: "Unit test stubs for OnMessage_QueueFull, Values_CounterOrder, OnMessage_MalformedJSON"
      exports: ["TestOnMessage_QueueFull", "TestValues_CounterOrder", "TestOnMessage_MalformedJSON"]
  key_links:
    - from: "processor/processor_test.go"
      to: "processor/processor.go"
      via: "package processor (same package test)"
      pattern: "package processor"
---

<objective>
Create failing unit test stubs for the three processor bugs that Plan 02 will fix (QUAL-01, QUAL-02, QUAL-03).

Purpose: Nyquist compliance — tests must exist and fail RED before implementation. Plan 02 depends on these stubs being in place so its verify commands run against real assertions.
Output: processor/processor_test.go with three failing tests.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@processor/processor.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test stubs for QUAL-01, QUAL-02, QUAL-03</name>
  <files>processor/processor_test.go</files>
  <behavior>
    - TestOnMessage_QueueFull: Create a Processor with queue size 1. Call OnMessage with a valid fruit payload to fill the queue. Call OnMessage again with a second valid fruit payload. Assert the second fruit is NOT in the queue (queue length still 1). Assert no panic or block occurred. [Currently fails: blocking send would deadlock or the test would need to detect a block — write the test to detect queue length stays at 1 via counter.]
    - TestValues_CounterOrder: Create a Processor, enqueue one fruit via OnMessage. Call Values(). Assert counter is 0 after the call (not -1 or 1). Assert returned row is non-nil. [Currently fails because counter is decremented before the read — the counter ordering is wrong in current code.]
    - TestOnMessage_MalformedJSON: Call OnMessage with a malformed JSON payload (e.g. `[]byte("not-json")`). Assert no fruit is added to the queue (counter stays 0). [Currently fails because malformed payload produces a zero-value fruit that is enqueued.]
  </behavior>
  <action>
    Create processor/processor_test.go in package processor (same-package test, no _test suffix on package name).

    Do NOT use the broken ValidateFruit helper from fruit_test.go — it panics on map fields. Write self-contained test logic.

    The test file needs a helper to construct a Processor for testing. Use Create() with a dummy timer channel:
    ```go
    func newTestProcessor(queueSize int) (Processor, chan bool) {
        timer := make(chan bool, 1)
        p := Create(timer, queueSize)
        return p, timer
    }
    ```

    For valid fruit payload in tests, use a minimal JSON that satisfies UnmarshalJSON. Inspect processor.go's OnMessage to see what topic prefix to use in the mock mqtt.Message. The fruit topic branch fires when the topic contains "Fruit". Use a mock mqtt.Message implementation:
    ```go
    type mockMsg struct {
        topic   string
        payload []byte
    }
    func (m mockMsg) Topic() string         { return m.topic }
    func (m mockMsg) Payload() []byte       { return m.payload }
    func (m mockMsg) Qos() byte             { return 0 }
    func (m mockMsg) Retained() bool        { return false }
    func (m mockMsg) MessageID() uint16     { return 0 }
    func (m mockMsg) Ack()                  {}
    func (m mockMsg) Duplicate() bool       { return false }
    ```

    For a minimal valid fruit payload: use `{}` (empty object — UnmarshalJSON will produce a zero-value Fruit but unmarshal succeeds). For the malformed payload test, use `[]byte("not valid json {{{")`

    TestOnMessage_QueueFull: fill queue with one fruit, then attempt second. Use a goroutine with a timeout to detect blocking (the test should NOT block). After the second OnMessage call, assert p.counter.Load() == 1.

    TestValues_CounterOrder: enqueue one fruit. Call p.Values(). Assert p.counter.Load() == 0 and err == nil.

    TestOnMessage_MalformedJSON: call OnMessage with malformed payload. Assert p.counter.Load() == 0.

    IMPORTANT: With the current (unfixed) code, TestOnMessage_QueueFull will block on the second send — wrap the second OnMessage call in a goroutine and use a select with a 100ms timeout. If it times out, the test passes the "blocking detected" assertion and the test itself passes as a stub (the stub documents the EXPECTED behavior, not the current behavior). Alternatively, since the current code blocks, structure tests so they call t.Skip("implementation pending") for the blocking case if you cannot reliably trigger the non-blocking path. The key requirement is: file compiles and `go test ./processor/... -run TestOnMessage_QueueFull` exits without hanging indefinitely.

    Safest approach for QueueFull stub: use a buffered channel approach — make the queue size 1, pre-fill it directly via the OnMessage path on a goroutine, then verify. Use a done channel to signal completion. If after 200ms the goroutine has not returned, assert false ("OnMessage blocked — QUAL-01 not yet fixed"). This structure fails RED correctly without hanging the test runner.

    Run `/usr/local/go/bin/go test ./processor/... -timeout 30s -run "TestOnMessage_QueueFull|TestValues_CounterOrder|TestOnMessage_MalformedJSON"` to confirm the file compiles. Tests should fail (RED) at assertion, not at compile time.
  </action>
  <verify>
    <automated>/usr/local/go/bin/go test ./processor/... -timeout 30s -run "TestOnMessage_QueueFull|TestValues_CounterOrder|TestOnMessage_MalformedJSON" 2>&1 | grep -E "FAIL|PASS|panic|does not compile" ; /usr/local/go/bin/go build ./... 2>&1</automated>
  </verify>
  <done>processor/processor_test.go exists. `go build ./...` succeeds. The three test functions exist and compile. Tests run (do not hang) and report FAIL (RED) — they fail at assertion, not at compile or panic.</done>
</task>

</tasks>

<verification>
`/usr/local/go/bin/go build ./...` exits 0.
`/usr/local/go/bin/go test ./processor/... -timeout 30s -run "TestOnMessage_QueueFull|TestValues_CounterOrder|TestOnMessage_MalformedJSON"` exits non-zero (tests fail RED) but does not hang.
processor/processor_test.go exists with all three test functions.
</verification>

<success_criteria>
Three failing unit tests exist and compile. The test runner completes within 30 seconds. Plan 02 can now use these tests in its verify commands.
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-cleanup/01-01-SUMMARY.md`
</output>

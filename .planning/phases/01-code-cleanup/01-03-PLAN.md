---
phase: 01-code-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - processor/processor.go
  - processor/grademap.go
  - processor/fruit.go
  - processor/fruit_test.go
  - breeze.go
  - go.mod
  - go.sum
autonomous: true
requirements:
  - QUAL-05

must_haves:
  truths:
    - "No commented-out dead code remains in any processor or breeze source file"
    - "github.com/gammazero/deque is removed from go.mod and go.sum"
    - "go build ./... and go test ./... are green after cleanup"
    - "Structural and architectural issues identified during review are documented in a code review note for the future milestone backlog"
  artifacts:
    - path: "processor/processor.go"
      provides: "Clean file — no commented deque fields or Create() comments"
    - path: "processor/grademap.go"
      provides: "Clean file — no commented vision grade block or unused commented mutation"
    - path: "processor/fruit.go"
      provides: "Clean file — no debug test line"
    - path: "processor/fruit_test.go"
      provides: "Clean file — no commented old API calls"
    - path: "breeze.go"
      provides: "Clean file — no commented connection or slog lines; log_level wiring is live (not commented)"
    - path: ".planning/phases/01-code-cleanup/01-CODE-REVIEW.md"
      provides: "Documented structural/architectural/idiomatic issues for future milestone backlog"
  key_links:
    - from: "go.mod"
      to: "processor/processor.go"
      via: "gammazero/deque removed once struct fields deleted"
      pattern: "gammazero"
---

<objective>
Remove all commented-out dead code from the processor and breeze source files, run go mod tidy to remove the dead deque dependency, and produce a code review note documenting structural issues for the future milestone backlog (QUAL-05).

Purpose: Clean slate for Phase 2 — no commented noise, no unused dependencies, and a written record of issues deferred from this phase.
Output: Cleaned source files, updated go.mod/go.sum, and 01-CODE-REVIEW.md.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@processor/processor.go
@processor/grademap.go
@processor/fruit.go
@processor/fruit_test.go
@breeze.go
@.planning/phases/01-code-cleanup/01-RESEARCH.md

<interfaces>
<!-- Dead code locations from research — exact targets for removal -->

processor/processor.go:
- Line 16: `// dequeue  *deque.Deque[Fruit]` — commented struct field, remove
- Line 17: `// current  Fruit` — commented struct field, remove
- Lines ~110-111: `// queue := new(deque.Deque[Fruit])` and `// queue.SetBaseCap(100)` in Create(), remove
- Lines ~116-117: `// Fruit{},` commented struct literal field in Create() return, remove

processor/grademap.go:
- Lines 122-131: Commented vision grade optimisation block inside Grade() — remove entire block
- Line 188: `// gm.Defect_grading_passes[k1] = v1` — remove

processor/fruit.go:
- Line 358: `// a.OtherDefects = []string{"ABCD"}` — debug test line, remove

processor/fruit_test.go:
- Line 93: `// got, err := FromJson(tt.args.val)` — remove
- Lines 101-103: `// if !reflect.DeepEqual(got, tt.want) {` commented block — remove
- Line 87: `// TODO: Add test cases.` — leave as-is (informational, not dead code)

breeze.go:
- Line 49: `// slog.SetLogLoggerLevel(slog.LevelDebug)` — remove (replaced by live log_level config from Plan 02)
- Line 85: `// conn, err := pgx.Connect(ctx, cfg.Database.URL)` — remove
- Line 92: `// config.MaxConnIdleTime =` — remove (incomplete commented config)
- Lines 131-132: `// slog.Info("New write")` — remove
- Lines 143-145: `// default:`, `// 	// do a piece of work`, `// 	time.Sleep(100 * time.Millisecond)` — remove old polling approach
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead code from all source files and run go mod tidy</name>
  <files>processor/processor.go, processor/grademap.go, processor/fruit.go, processor/fruit_test.go, breeze.go, go.mod, go.sum</files>
  <action>
    Remove each dead code location listed in the interfaces block above. This is a pure deletion pass — do not modify working logic, only remove comment lines.

    Order of operations:
    1. processor/processor.go: Remove the two commented struct fields (deque, current). Remove the two commented lines in Create() body (queue := new..., queue.SetBaseCap...). Remove the commented Fruit{} line in Create() return. After removing deque struct fields, check whether the `github.com/gammazero/deque` import is still referenced anywhere in processor.go — it should not be; remove it from the import block if present.
    2. processor/grademap.go: Remove the commented vision grade optimisation block (lines 122-131). Remove the commented mutation line (line 188).
    3. processor/fruit.go: Remove the debug test line (line 358).
    4. processor/fruit_test.go: Remove the two commented old API call lines (line 93) and the commented assertion block (lines 101-103). Leave `// TODO: Add test cases.` in place.
    5. breeze.go: Remove all five dead comment locations listed above.

    After all deletions, run:
    ```
    /usr/local/go/bin/go mod tidy
    ```
    This removes `github.com/gammazero/deque` from go.mod and go.sum since it has no active usages remaining.

    Then verify:
    ```
    /usr/local/go/bin/go build ./...
    /usr/local/go/bin/go test ./... -timeout 60s
    ```

    SCOPE GUARD: Do NOT refactor working logic, rename variables, change function signatures, or fix the pre-existing TestFromJson map panic in fruit_test.go. Dead code removal only.

    PITFALL GUARD (from research): Run `go mod tidy` AFTER all dead code is removed, not before. If you run it before removing the deque struct fields, the import may still appear referenced and the module will not be removed.
  </action>
  <verify>
    <automated>/usr/local/go/bin/go build ./... 2>&1 && /usr/local/go/bin/go test ./... -timeout 60s 2>&1 | tail -10 && grep -rn "gammazero" /home/ben/github.com/bellgrove/breeze/go.mod && echo "FAIL: gammazero still in go.mod" || echo "PASS: gammazero removed"</automated>
  </verify>
  <done>`go build ./...` exits 0. `go test ./...` passes. `grep gammazero go.mod` finds nothing. No commented-out deque, slog, or old-connection lines remain in the modified files.</done>
</task>

<task type="auto">
  <name>Task 2: Write code review note documenting deferred structural issues</name>
  <files>.planning/phases/01-code-cleanup/01-CODE-REVIEW.md</files>
  <action>
    Read all five source files (processor/processor.go, processor/grademap.go, processor/fruit.go, processor/fruit_test.go, breeze.go) and identify structural, architectural, or idiomatic issues that are real concerns but explicitly OUT OF SCOPE for Phase 1. Do not fix them — document them.

    The known issues to document (from research):
    1. `p.timer <- true` blocking when timer channel buffer is full: timer is `make(chan bool, 1)`. If the batch goroutine is slow, a second fruit arrival inside the `case p.queue <- f:` branch will block on `p.timer <- true`. The QUAL-01 non-blocking select only guards the queue send, not the timer send. Fix options: a second select for the timer send, or making timer capacity match queue size.
    2. TestFromJson pre-existing panic: `fruit_test.go` panics on `reflect.Value.Equal` for `map[string]interface{}` fields (MCenterOffsets etc.). This is a test bug, not a source bug. Future work: rewrite comparison using `reflect.DeepEqual` or JSON round-trip.
    3. Any additional idiomatic Go issues observed during the dead code removal pass — document each with file + approximate line, description, and suggested fix.

    Format the file as a plain Markdown document under `.planning/phases/01-code-cleanup/01-CODE-REVIEW.md`. Structure:
    - Header: "Phase 1 Code Review — Deferred Issues"
    - Date
    - One section per issue: issue name, location (file + line), description, impact, suggested fix, priority (low/medium/high for next milestone)
    - A closing note stating these are deferred to the next milestone backlog and should be added to RETROSPECTIVE.md

    This document is for the user's review and future planning — it is not a task list for this phase.
  </action>
  <verify>
    <automated>ls /home/ben/github.com/bellgrove/breeze/.planning/phases/01-code-cleanup/01-CODE-REVIEW.md && echo "PASS: file exists"</automated>
  </verify>
  <done>01-CODE-REVIEW.md exists with at minimum the two known issues (timer blocking, TestFromJson panic) documented. File is readable Markdown.</done>
</task>

</tasks>

<verification>
`/usr/local/go/bin/go build ./...` exits 0.
`/usr/local/go/bin/go test ./... -timeout 60s` passes.
`grep -rn "gammazero" go.mod` — no matches.
`grep -rn "// dequeue\|// queue :=\|// conn, err\|// slog.SetLogLoggerLevel" processor/ breeze.go` — no matches.
`.planning/phases/01-code-cleanup/01-CODE-REVIEW.md` exists.
</verification>

<success_criteria>
All commented-out dead code removed. gammazero/deque removed from go.mod. go build and go test are green. Code review note written documenting deferred issues for next milestone.
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-cleanup/01-03-SUMMARY.md`
</output>

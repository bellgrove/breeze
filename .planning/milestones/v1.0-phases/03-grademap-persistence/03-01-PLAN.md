---
phase: 03-grademap-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - processor/grademap_diff.go
  - processor/grademap_diff_test.go
  - processor/processor_test.go
  - breeze_test.go
autonomous: true
requirements:
  - GRAD-01
  - GRAD-02
  - GRAD-03

must_haves:
  truths:
    - "`go test ./... -count=1` compiles and runs (all new tests fail RED, suite does not error)"
    - "TestDiffGrademaps exists as a failing stub for GRAD-03"
    - "TestOnMessage_GrademapChannel exists as a failing stub for GRAD-02"
    - "TestGrademapSchema exists as a failing stub for GRAD-01"
  artifacts:
    - path: "processor/grademap_diff.go"
      provides: "GrademapChange type and DiffGrademaps stub (returns nil)"
      exports: ["GrademapChange", "DiffGrademaps"]
    - path: "processor/grademap_diff_test.go"
      provides: "TestDiffGrademaps table-driven test stub"
      contains: "TestDiffGrademaps"
    - path: "processor/processor_test.go"
      provides: "TestOnMessage_GrademapChannel stub added to existing file"
      contains: "TestOnMessage_GrademapChannel"
    - path: "breeze_test.go"
      provides: "TestGrademapSchema stub"
      contains: "TestGrademapSchema"
  key_links:
    - from: "processor/grademap_diff_test.go"
      to: "processor/grademap_diff.go"
      via: "DiffGrademaps function call"
      pattern: "DiffGrademaps"
    - from: "processor/processor_test.go"
      to: "processor/processor.go"
      via: "GradeCh() method call"
      pattern: "GradeCh"
---

<objective>
Write the Nyquist wave-0 failing test stubs for Phase 3 — grademap persistence.

Purpose: Tests must exist before implementation so every subsequent plan has automated verification available from the start. All three stubs fail RED immediately; implementation plans make them GREEN.

Output: Four files (one new, two modified, one new) providing failing tests for GRAD-01 schema DDL, GRAD-02 non-blocking channel, and GRAD-03 diff function.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-grademap-persistence/03-RESEARCH.md
@.planning/phases/03-grademap-persistence/03-VALIDATION.md

<interfaces>
<!-- Existing processor package — executor must not break these -->

From processor/processor.go:
```go
type Processor struct {
    timer    chan bool
    queue    chan Fruit
    counter  atomic.Int32
    grademap Grademap
}
// gradeCh field does NOT exist yet — Plan 02 adds it

func (p *Processor) OnMessage(client mqtt.Client, msg mqtt.Message)
func Create(next chan bool, queueSize int) Processor
func DrainFruits(p *Processor) []Fruit
```

From processor/processor_test.go (existing helper — reuse it):
```go
// mockMsg implements mqtt.Message for testing
type mockMsg struct { topic string; payload []byte }
// All mqtt.Message methods implemented

func newTestProcessorSmallQueue(queueSize int) Processor
// Creates Processor bypassing Create(), with buffered timer
```

From breeze_test.go (check existing content before adding):
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create grademap_diff.go stub and failing TestDiffGrademaps</name>
  <files>processor/grademap_diff.go, processor/grademap_diff_test.go</files>
  <behavior>
    - DiffGrademaps(nil, next) returns at least one GrademapChange (all fields treated as new) — stub returns nil so test FAILS RED
    - DiffGrademaps(prev, next) where Name changed returns change with EntityType="grademap", Field="Name", OldValue=old, NewValue=new — stub returns nil so test FAILS RED
    - DiffGrademaps(prev, next) where nothing changed returns empty slice — stub returns nil so test FAILS RED (nil != empty but that is acceptable for now; the stub just needs to compile)
  </behavior>
  <action>
    Create `processor/grademap_diff.go`:
    - Package `processor`
    - Define `GrademapChange` struct with fields: `EntityType string`, `EntityName string`, `Field string`, `OldValue string`, `NewValue string`
    - Define `DiffGrademaps(prev, next map[string]any) []GrademapChange` — body is `return nil` (stub; implementation in Plan 02)

    Create `processor/grademap_diff_test.go`:
    - Package `processor`
    - Write `TestDiffGrademaps` as a table-driven test with at least three cases:
      1. `prev=nil, next={"Name": "GM1"}` — expect at least one change with Field="Name", NewValue="GM1", OldValue=""
      2. `prev={"Name": "GM1"}, next={"Name": "GM2"}` — expect one change with EntityType="grademap", Field="Name", OldValue="GM1", NewValue="GM2"
      3. `prev={"Name": "GM1"}, next={"Name": "GM1"}` — expect zero changes
    - For each case, call `DiffGrademaps(tc.prev, tc.next)` and check the result
    - Test MUST FAIL RED with the nil stub — do not add `t.Skip()`
  </action>
  <verify>
    <automated>go test ./processor/... -run TestDiffGrademaps -count=1 2>&amp;1 | grep -E "FAIL|PASS|compile"</automated>
  </verify>
  <done>File compiles. `go test ./processor/... -run TestDiffGrademaps -count=1` exits non-zero (FAIL RED) — stub returns nil, tests assert non-nil changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TestOnMessage_GrademapChannel stub to processor_test.go</name>
  <files>processor/processor_test.go</files>
  <behavior>
    - After a grademap-topic OnMessage call, proc.GradeCh() returns a channel with one item (the raw payload) — stub: GradeCh() does not exist yet, so test fails to compile → FAIL RED
    - OnMessage returns without blocking when gradeCh is full (buffer=1, already occupied) — same compile failure
  </behavior>
  <action>
    Append `TestOnMessage_GrademapChannel` to the existing `processor/processor_test.go` file (do NOT replace the file — read it first and append).

    The test:
    1. Constructs a `newTestProcessorSmallQueue(5)` Processor
    2. Calls `p.OnMessage(nil, mockMsg{topic: "tomra/211632/grademap", payload: []byte(`{"Name":"GM1"}`)})` in a goroutine with a 200ms timeout — asserts it returns without blocking
    3. Calls `p.GradeCh()` and checks that a value can be received from it (len of channel >= 1)
    4. Verifies the received bytes equal `[]byte(`{"Name":"GM1"}`)` — confirming the payload was copied (not the same slice)

    Note: `GradeCh()` does not exist on `Processor` yet — the test will fail to compile, which is the intended RED state. The `newTestProcessorSmallQueue` helper also does not initialise `gradeCh`, so the test will panic or compile-fail until Plan 02 fixes both. Add a comment: `// GradeCh() added by Plan 02 — this test is intentionally RED until then`.
  </action>
  <verify>
    <automated>go test ./processor/... -run TestOnMessage_GrademapChannel -count=1 2>&amp;1 | grep -E "FAIL|compile error|undefined"</automated>
  </verify>
  <done>`processor/processor_test.go` compiles as a file (existing tests still pass), but `TestOnMessage_GrademapChannel` references `GradeCh()` which does not exist — the package fails to compile, confirming RED state for the new test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add TestGrademapSchema stub to breeze_test.go</name>
  <files>breeze_test.go</files>
  <behavior>
    - `grademapDDL` constant contains `CREATE TABLE breeze_grademap` — stub: constant does not exist yet, test FAILS RED
    - `grademapChangesDDL` constant contains `CREATE TABLE breeze_grademap_changes` — same
    - `grademapChangesDDL` contains `REFERENCES breeze_grademap` — same
  </behavior>
  <action>
    Read `breeze_test.go` first. Append `TestGrademapSchema` to it.

    The test:
    1. Checks that a package-level constant or variable `grademapDDL` (type `string`) contains the substring `"CREATE TABLE breeze_grademap"` — use `strings.Contains`
    2. Checks that `grademapChangesDDL` contains `"CREATE TABLE breeze_grademap_changes"`
    3. Checks that `grademapChangesDDL` contains `"REFERENCES breeze_grademap"` — confirming the FK is declared

    These constants (`grademapDDL`, `grademapChangesDDL`) will be defined in `breeze.go` or a new `schema_ddl.go` file by Plan 03. For now they do not exist — the test fails to compile (RED state). Add a comment: `// grademapDDL and grademapChangesDDL defined by Plan 03 — intentionally RED until then`.

    Import `strings` in the test file if not already imported.
  </action>
  <verify>
    <automated>go test . -run TestGrademapSchema -count=1 2>&amp;1 | grep -E "FAIL|compile error|undefined"</automated>
  </verify>
  <done>`breeze_test.go` references `grademapDDL` and `grademapChangesDDL` which do not exist — package fails to compile, confirming RED state.</done>
</task>

</tasks>

<verification>
After all three tasks:

```
go test ./... -count=1 2>&1
```

Expected outcome: compile errors referencing `GradeCh` (undefined) and `grademapDDL` / `grademapChangesDDL` (undefined). This is correct — the test stubs are intentionally ahead of the implementation.

The only tests that must pass (GREEN) are the pre-existing Phase 1 and Phase 2 tests — confirm they are not broken by running:

```
go test ./processor/... -run "TestOnMessage_QueueFull|TestValues_CounterOrder|TestOnMessage_MalformedJSON|TestDrainFruits" -count=1
```
</verification>

<success_criteria>
- `processor/grademap_diff.go` exists with `GrademapChange` struct and `DiffGrademaps` stub
- `processor/grademap_diff_test.go` exists with three-case `TestDiffGrademaps`
- `processor/processor_test.go` has `TestOnMessage_GrademapChannel` appended
- `breeze_test.go` has `TestGrademapSchema` appended
- Pre-existing tests (QUAL-*, RECO-*) remain GREEN
- New tests are RED (compile errors or assertion failures) — this is the intended state
</success_criteria>

<output>
After completion, create `.planning/phases/03-grademap-persistence/03-01-SUMMARY.md`
</output>

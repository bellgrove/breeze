---
phase: 04-timing-reconciliation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - breeze_test.go
autonomous: true
requirements:
  - TIME-01
  - TIME-02

must_haves:
  truths:
    - "`go test ./...` compiles and all new test functions exist"
    - "Tests for `resolveGrademapID` are RED — function does not exist yet"
    - "Tests for config propagation delay parsing are RED — field does not exist yet"
    - "Tests for `writeBatch` grademap ID passing are RED — signature not updated yet"
  artifacts:
    - path: "breeze_test.go"
      provides: "Wave 0 test stubs for TIME-01 and TIME-02"
      contains: "TestResolveGrademapID"
  key_links:
    - from: "breeze_test.go"
      to: "breeze.go"
      via: "calls resolveGrademapID (will exist after Plan 02)"
      pattern: "resolveGrademapID"
---

<objective>
Write failing test stubs (Wave 0 / Nyquist compliance) for the Phase 4 timing reconciliation features.

Purpose: Establish the test scaffold before any implementation. All seven test functions must compile but produce RED failures because the production functions do not exist yet. This satisfies the Nyquist requirement from 04-VALIDATION.md.

Output: Seven new test functions appended to `breeze_test.go`, covering TIME-01 (AS-OF lookup) and TIME-02 (configurable delay offset).
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-timing-reconciliation/04-RESEARCH.md
@.planning/phases/04-timing-reconciliation/04-VALIDATION.md

<interfaces>
<!-- Existing test patterns from breeze_test.go. Use these conventions for new stubs. -->

// Closed-pool pattern for exercising error paths without a real DB:
pool, err := pgxpool.New(ctx, "postgres://invalid:invalid@localhost:1/doesnotexist?connect_timeout=1")
if pool != nil { pool.Close() }

// Functions to be called in these stubs (do not exist yet — stubs will fail to compile
// until Plan 02 adds them; use t.Skip or compile-time approach):
//   resolveGrademapID(ctx, pool, fruitTime time.Time, delay time.Duration) (int64, bool, error)
//   writeBatch(ctx, pool, batch []processor.Fruit, delay time.Duration) error  // signature changes in Plan 03
//   Config.GrademapPropagationDelayStr string  // field added in Plan 02
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave 0 test stubs for resolveGrademapID (TIME-01)</name>
  <files>breeze_test.go</files>
  <behavior>
    - TestResolveGrademapID_NoRows: closed pool returns false/nil (pgx.ErrNoRows branch) — no grademap yet means (0, false, nil)
    - TestResolveGrademapID_ZeroDelay: calling with delay=0 passes fruitTime unmodified as the anchor; verify no error from the adjustment itself (pool error is expected)
    - TestResolveGrademapID: correct function signature accepted — function exists with signature (ctx, pool, time.Time, time.Duration) (int64, bool, error)
    - TestWriteBatch_GrademapID: writeBatch with the updated signature (adding delay time.Duration param) returns an error on closed pool and does not panic
  </behavior>
  <action>
    Append four test functions to `breeze_test.go`. These are RED tests — they reference
    `resolveGrademapID` and the updated `writeBatch` signature which do not exist until Plan 02/03.

    Use the same closed-pool pattern as `TestTransactionalWrite`:
      pool, _ := pgxpool.New(ctx, "postgres://invalid:invalid@localhost:1/doesnotexist?connect_timeout=1")
      if pool != nil { pool.Close() }

    TestResolveGrademapID_NoRows:
      - Call resolveGrademapID(ctx, pool, time.Now(), 0)
      - Pool is closed so QueryRow returns an error; function must return (0, false, nil) for
        pgx.ErrNoRows or a wrapped error for other errors — just assert err is non-nil OR
        (id==0 && !ok) to cover both branches.
      - Comment: "stub — RED until Plan 02 adds resolveGrademapID"

    TestResolveGrademapID_ZeroDelay:
      - Call resolveGrademapID(ctx, pool, time.Now(), 0)
      - Assert the call does not panic (pool error expected; delay=0 must not shift the anchor)
      - Comment: "stub — RED until Plan 02 adds resolveGrademapID"

    TestResolveGrademapID:
      - Call resolveGrademapID(ctx, pool, time.Now(), 30*time.Second)
      - Assert call compiles and returns (int64, bool, error) triple
      - Comment: "stub — RED until Plan 02 adds resolveGrademapID"

    TestWriteBatch_GrademapID:
      - Call writeBatch(ctx, pool, []processor.Fruit{{CarrierId:"test"}}, 0)
      - (writeBatch signature gains `delay time.Duration` in Plan 03; stub will fail to
        compile until then — that is correct Wave 0 behaviour)
      - Assert err != nil (closed pool)
      - Comment: "stub — RED until Plan 03 updates writeBatch signature"

    Import: add "errors" if needed. All imports already present in the file can be reused.
  </action>
  <verify>
    <automated>cd /home/ben/github.com/bellgrove/breeze && go build ./... 2>&1 | head -20</automated>
  </verify>
  <done>
    Four new test functions exist in breeze_test.go. `go build ./...` on the existing production
    code (not test files) still compiles clean. Test compilation will be RED until Plan 02/03
    provide the implementations — which is the correct Wave 0 state.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Wave 0 test stubs for config delay and delay offset (TIME-02)</name>
  <files>breeze_test.go</files>
  <behavior>
    - TestConfig_PropagationDelay: Config struct has a GrademapPropagationDelayStr string field; "30s" parses without error via time.ParseDuration
    - TestConfig_PropagationDelayZero: empty string or "0s" parses to zero duration (no offset)
    - TestResolveGrademapID_WithDelay: delay=30s shifts the anchor time backward by 30s — verify by checking adjustedTime = fruitTime.Add(-delay); since no real DB, just assert the function call does not panic
  </behavior>
  <action>
    Append three more test functions to `breeze_test.go`.

    TestConfig_PropagationDelay:
      - Construct a Config{} with GrademapPropagationDelayStr: "30s"
      - Call time.ParseDuration(cfg.GrademapPropagationDelayStr) — assert no error and result == 30*time.Second
      - Comment: "stub — RED until Plan 02 adds GrademapPropagationDelayStr to Config"

    TestConfig_PropagationDelayZero:
      - Test two sub-cases:
        1. GrademapPropagationDelayStr == "" → treat as zero delay (no parse needed; zero is valid)
        2. GrademapPropagationDelayStr == "0s" → time.ParseDuration returns 0, no error
      - Assert both produce zero time.Duration
      - Comment: "stub — RED until Plan 02 adds GrademapPropagationDelayStr to Config"

    TestResolveGrademapID_WithDelay:
      - fruitTime := time.Now()
      - delay := 30 * time.Second
      - Call resolveGrademapID(ctx, pool, fruitTime, delay)
      - Assert call does not panic (pool error is expected)
      - Optionally: demonstrate the math inline (adjustedTime := fruitTime.Add(-delay); assert
        adjustedTime.Before(fruitTime)) to document the invariant without needing a real DB
      - Comment: "stub — RED until Plan 02 adds resolveGrademapID"

    All three functions reference Config.GrademapPropagationDelayStr which does not exist until
    Plan 02 — they will fail to compile, which is correct Wave 0 behaviour.
  </action>
  <verify>
    <automated>cd /home/ben/github.com/bellgrove/breeze && go build ./... 2>&1 | head -20</automated>
  </verify>
  <done>
    Seven total test stubs exist in breeze_test.go (4 from Task 1 + 3 from Task 2).
    Production code (`go build ./...`) still compiles. Test compilation is RED — correct.
    Wave 0 requirements from 04-VALIDATION.md are fully satisfied.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `go build ./...` passes (production code unchanged)
- `grep -c "^func Test" breeze_test.go` returns at least 10 (7 existing + 7 new... wait, check 7 new exist)
- `grep "TestResolveGrademapID\|TestWriteBatch_GrademapID\|TestConfig_PropagationDelay" breeze_test.go` shows all 7 stubs
</verification>

<success_criteria>
All seven Wave 0 test stubs exist in breeze_test.go. Production code compiles. Tests are RED (expected). Nyquist compliance is satisfied for Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/04-timing-reconciliation/04-01-SUMMARY.md`
</output>
